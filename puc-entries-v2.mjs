import fs from'node:fs/promises';
const BASE='https://dp-myfit-test-function-v2.azurewebsites.net',cfg=JSON.parse(await fs.readFile('players.json','utf8')),players=cfg.players||[],data=JSON.parse(await fs.readFile('data.json','utf8')),norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9]+/g,' ').trim(),errors=[],queries=[],all=[];
async function post(path,body){const r=await fetch(BASE+path,{method:'POST',headers:{'content-type':'application/json','user-agent':'Mozilla/5.0 CourtWatch/6.0','origin':'https://www.fitp.it','referer':'https://www.fitp.it/Tornei/Ricerca-tornei'},body:JSON.stringify(body)}),text=await r.text();if(!r.ok||!text)throw Error(`${r.status} ${text.slice(0,160)}`);return JSON.parse(text)}
const iso=v=>{const s=String(v||'');let m=s.match(/^(20\d{2})-(\d{2})-(\d{2})/);if(m)return`${m[1]}-${m[2]}-${m[3]}`;m=s.match(/(\d{1,2})\D(\d{1,2})\D(20\d{2})/);return m?`${m[3]}-${m[2].padStart(2,'0')}-${m[1].padStart(2,'0')}`:null},it=d=>`${String(d.getUTCDate()).padStart(2,'0')}/${String(d.getUTCMonth()+1).padStart(2,'0')}/${d.getUTCFullYear()}`,today=new Date(),todayIso=today.toISOString().slice(0,10),limitDate=new Date(Date.now()+240*864e5),limit=limitDate.toISOString().slice(0,10),terms=['','JUNIOR','UNDER','U10','U11','U12','U13','U14','U16','U18','NEXT GEN','CAMPIONATI','QUALIFICAZIONE','MEMORIAL','CIRCUITO','COPPA','MASTER','GIOVANILE','TORNEO'];
function payload(text,start,end,skip){return{guid:'',profilazione:'',freetext:text,id_regione:'',id_provincia:'',id_stato:'',id_disciplina:'',sesso:'',data_inizio:it(start),data_fine:it(end),tipo_competizione:'',categoria_eta:'',id_classifica:'',classifica:'',massimale_montepremi:'',id_area_regionale:'',ambito:'',rowstoskip:skip,fetchrows:100,sortcolumn:'',sortorder:''}}
for(let start=new Date(Date.UTC(today.getUTCFullYear(),today.getUTCMonth(),today.getUTCDate()));start<=limitDate;start=new Date(start.getTime()+14*864e5)){const end=new Date(Math.min(limitDate.getTime(),start.getTime()+13*864e5));for(const term of terms)for(const skip of[0,100])try{const r=await post('/api/v3/tornei/puc/list',payload(term,start,end,skip)),rows=r.competizioni||[];queries.push({term:term||'ALL',start:it(start),end:it(end),skip,record:Number(r.record||rows.length),rows:rows.length,official:rows.filter(x=>Number(x.cod_fonte)===1&&x.guid&&x.guid!=='11111111-1111-1111-1111-111111111111').length});all.push(...rows.filter(x=>Number(x.cod_fonte)===1&&x.guid&&x.guid!=='11111111-1111-1111-1111-111111111111'))}catch(e){errors.push(`list ${term||'ALL'} ${it(start)} ${skip}: ${e.message}`)}}
let api={competitions:[]};try{api=JSON.parse(await fs.readFile('puc-api.json','utf8'))}catch{}for(const c of api.competitions||[])if(c.guid)all.push({guid:c.guid,nome_torneo:c.name});for(const t of data.tournaments||[])if(t.sourceId==='fitp-puc'&&t.competitionId)all.push({guid:t.competitionId,nome_torneo:t.name,data_inizio:t.startDate,data_fine:t.endDate,citta:t.location});const competitions=[...new Map(all.map(c=>[String(c.guid).toUpperCase(),c])).values()].filter(c=>{const s=iso(c.data_inizio||c.startDate),e=iso(c.data_fine||c.endDate);return(!e||e>=todayIso)&&(!s||s<=limit)}),found=[],rankings={},details=[];let cursor=0;
async function worker(){while(cursor<competitions.length){const c=competitions[cursor++];try{const d=await post('/api/v3/puc/competizione/dettaglio',{competitionUid:c.guid}),hits=new Map;for(const draw of d?.Tournaments||[])for(const q of draw.Participants||[]){const full=norm(`${q.Name||''} ${q.Surname||''}`);if(q.Ranking&&full)rankings[full]=q.Ranking;const p=players.find(x=>norm(x.name)===full||(x.aliases||[]).some(a=>norm(a)===full));if(!p)continue;const h=hits.get(p.id)||{player:p,draws:[],ranking:q.Ranking,membershipCard:q.MembershipCard};h.draws.push(draw.TournamentDescription);hits.set(p.id,h)}if(hits.size)details.push({guid:c.guid,name:d?.Description||c.nome_torneo,players:[...hits.values()].map(h=>h.player.name)});for(const h of hits.values()){const start=iso(d?.From||c.data_inizio),end=iso(d?.To||c.data_fine),name=d?.Description||c.nome_torneo||'Torneo FITP',location=[d?.Municipality||c.citta,d?.Province||c.provincia].filter(Boolean).join(' '),existing=(data.tournaments||[]).find(t=>t.playerId===h.player.id&&String(t.competitionId).toUpperCase()===String(c.guid).toUpperCase()),value={key:existing?.key||`fitp-${String(c.guid).toLowerCase()}|${h.player.id}`,playerId:h.player.id,playerName:h.player.name,name,location:location||existing?.location||'',sourceId:'fitp-puc',sourceName:'P.U.C. FITP',url:'https://www.fitp.it/Tornei/Dettaglio-Competizione?competitionId='+encodeURIComponent(c.guid),startDate:start||existing?.startDate,endDate:end||existing?.endDate,status:end&&end<todayIso?'finished':start&&start>todayIso?'upcoming':'active',competitionId:c.guid,draws:[...new Set(h.draws)],playerRanking:h.ranking,membershipCard:h.membershipCard,lastSeen:new Date().toISOString()};if(existing)Object.assign(existing,value);else(data.tournaments||=[]).push(value);found.push({player:h.player.name,competition:name,competitionId:c.guid,draws:value.draws,startDate:value.startDate,endDate:value.endDate})}}catch(e){errors.push(`detail ${c.guid}: ${e.message}`)}}}
await Promise.all(Array.from({length:24},worker));data.generatedAt=new Date().toISOString();data.players=players.map(p=>({id:p.id,name:p.name,club:p.club||'',circuits:p.circuits||[]}));data.fitpRankings=rankings;data.fitpEntryDiscovery={lastRun:data.generatedAt,status:errors.length?'partial':'complete',coverageUntil:limit,method:'Finestre ufficiali P.U.C. di 14 giorni, doppia pagina, categorie junior',queryWindows:Math.ceil(241/14),queries:queries.length,competitionsChecked:competitions.length,found,details,errors:errors.slice(0,150)};await fs.writeFile('data.json',JSON.stringify(data,null,2)+'\n');await fs.writeFile('puc-entries.json',JSON.stringify(data.fitpEntryDiscovery,null,2)+'\n');console.log(JSON.stringify({status:data.fitpEntryDiscovery.status,queries:queries.length,competitionsChecked:competitions.length,found:found.length,errors:errors.length},null,2));if(!competitions.length)process.exitCode=2;
