name: Court Watch action execution probe
on:
  push:
    paths:
      - '.github/workflows/courtwatch-action-probe.yml'
permissions: {contents: read}
concurrency:
  group: court-watch-action-probe
  cancel-in-progress: true
jobs:
  cancel-legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Obsolete FITP probe cancelled; no data rebuilt or published.'
