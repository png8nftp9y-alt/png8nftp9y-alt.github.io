name: Court Watch v3 FITP player details script

on:
  workflow_dispatch:
  push:
    paths:
      - 'courtwatch-v3-fitp-player-details-script-trigger.json'
      - 'src/v3/diagnose-fitp-player-details-script.mjs'
      - '.github/workflows/courtwatch-v3-fitp-player-details-script.yml'

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: |
          set +e
          node src/v3/diagnose-fitp-player-details-script.mjs > courtwatch-v3-fitp-player-details-script-run.log 2>&1
          echo "scriptExit=$?" >> courtwatch-v3-fitp-player-details-script-run.log
      - run: |
          git config user.name courtwatch-bot
          git config user.email courtwatch-bot@users.noreply.github.com
          git add dist/v3/fitp_player_details_script_diagnostic.json courtwatch-v3-fitp-player-details-script-run.log
          git commit -m "Run Court Watch v3 FITP player details script diagnostic" || echo "No changes"
          git push
