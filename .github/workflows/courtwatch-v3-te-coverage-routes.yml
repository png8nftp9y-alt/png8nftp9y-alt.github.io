name: Court Watch v3 Tennis Europe coverage routes

on:
  workflow_dispatch:
  push:
    paths:
      - 'courtwatch-v3-te-coverage-routes-trigger.json'
      - 'src/v3/diagnose-tennis-europe-coverage-routes.mjs'
      - '.github/workflows/courtwatch-v3-te-coverage-routes.yml'

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: |
          set +e
          node src/v3/diagnose-tennis-europe-coverage-routes.mjs > courtwatch-v3-te-coverage-routes-run.log 2>&1
          echo "teCoverageRoutesExit=$?" >> courtwatch-v3-te-coverage-routes-run.log
      - run: |
          git config user.name courtwatch-bot
          git config user.email courtwatch-bot@users.noreply.github.com
          git add dist/v3/tennis_europe_coverage_routes_diagnostic.json courtwatch-v3-te-coverage-routes-run.log
          git commit -m "Run Court Watch v3 Tennis Europe coverage routes diagnostic" || echo "No changes"
          git push
