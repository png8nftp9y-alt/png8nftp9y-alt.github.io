name: Court Watch v3 FITP detail bracket audit
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/v3/audit-fitp-detail-bracket-sources.mjs'
      - 'courtwatch-v3-fitp-detail-bracket-audit-trigger.json'
      - '.github/workflows/courtwatch-v3-fitp-detail-bracket-audit.yml'
permissions: {contents: write}
concurrency: {group: court-watch-v3-fitp-detail-bracket-audit,cancel-in-progress: true}
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - name: Audit FITP detail and bracket data
        run: |
          node src/v3/audit-fitp-detail-bracket-sources.mjs > courtwatch-v3-fitp-detail-bracket-audit-run.log 2>&1
          cat courtwatch-v3-fitp-detail-bracket-audit-run.log
      - name: Commit audit output
        if: always()
        run: |
          git config user.name courtwatch-bot
          git config user.email courtwatch-bot@users.noreply.github.com
          git add dist/v3/source_fitp_detail_bracket_audit.json courtwatch-v3-fitp-detail-bracket-audit-run.log
          git commit -m "Build Court Watch v3 FITP detail bracket audit" || exit 0
          git pull --rebase origin main || true
          git push origin HEAD:main || true
