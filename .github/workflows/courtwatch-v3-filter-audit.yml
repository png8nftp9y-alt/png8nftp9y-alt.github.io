name: Court Watch v3 FITP filter audit
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/v3/audit-fitp-filters.mjs'
      - 'courtwatch-v3-filter-audit-trigger.json'
      - '.github/workflows/courtwatch-v3-filter-audit.yml'
permissions: {contents: write}
concurrency: {group: court-watch-v3-filter-audit,cancel-in-progress: true}
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - name: Run FITP filter audit
        run: |
          set +e
          node src/v3/audit-fitp-filters.mjs > courtwatch-v3-filter-audit-run.log 2>&1
          code=$?
          echo "exitCode=$code" >> courtwatch-v3-filter-audit-run.log
          cat courtwatch-v3-filter-audit-run.log
          exit $code
      - name: Commit FITP filter audit
        if: always()
        run: |
          git config user.name courtwatch-bot
          git config user.email courtwatch-bot@users.noreply.github.com
          git add dist/v3/source_fitp_filter_audit.json courtwatch-v3-filter-audit-run.log .github/workflows/courtwatch-v3-filter-audit.yml
          git commit -m "Build Court Watch v3 FITP filter audit" || exit 0
          git pull --rebase origin main || true
          git push origin HEAD:main || true
