name: Monitor official tennis portals
on:
  workflow_dispatch:
permissions: {contents: write}
concurrency: {group: court-watch-portals-disabled,cancel-in-progress: true}
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Continuous sync is handled by full-circuit-discovery.yml."
