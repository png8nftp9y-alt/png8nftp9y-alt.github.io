name: Court Watch v3 legacy builder disabled
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled: v3 is now built only by src/v3 independent engines.'
