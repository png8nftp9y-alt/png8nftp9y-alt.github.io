name: Tennis Europe entries legacy
on:
  workflow_dispatch:
permissions: {contents: read}
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled: full-circuit-discovery.yml is the only Court Watch data writer."
