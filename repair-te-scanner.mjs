import fs from'node:fs/promises';let s=await fs.readFile('te-entries.mjs','utf8');const base="['https:','','te.tournamentsoftware.com'].join('/')";s=s.replace("page.goto('{{https://te.tournamentsoftware.com/tournaments',{waitUntil:'domcontentloaded',timeout:60000}}})",`page.goto(${base}+'/tournaments',{waitUntil:'domcontentloaded',timeout:60000})`);s=s.replace("url=`{{https://te.tournamentsoftware.com/find?StartDate=${f}}}&EndDate=${t}&StatusFilterID=0`",`url=${base}+'/find?StartDate='+f+'&EndDate='+t+'&StatusFilterID=0'`);if(s.includes("'{{https" )||s.includes('`{{https'))throw Error('Malformed URL remains');await fs.writeFile('te-entries.mjs',s);console.log('repaired');
