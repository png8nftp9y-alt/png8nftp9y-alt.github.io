# Court Watch v3 — report completo di progetto e passaggio di consegne — Parte 2

Continua da `docs/CourtWatch_v3_handoff_report.md` (revisione storica `2026-08-31.53`). Il file storico resta invariato e costituisce la Parte 1.

Revisione documento: **2026-09-01.54**

- 1 settembre 2026 — Audit Cloudflare D1/R2 certificato verde. Il workflow `.github/workflows/courtwatch-cloudflare-access-audit.yml` ha completato con successo il primo audit aggiornato, run `33444511226`, e il successivo audit con fingerprint, run `33444749195`; tutti gli step del secondo run sono risultati `success`. D1: 20 tabelle applicative, `integrity_check=ok`, fingerprint deterministico globale SHA-256 `9640bbe60ff23bce2b55a970b6bcee3fddd122ab4ca531e6f9df61220459e267`. R2: tutti i sei prefissi richiesti presenti e verificati: `fitp/cache` 9 oggetti, `itf/database` 337, `itf/history-draws` 20, `tennis-europe/cache` 9, `tennis-europe/oop-history` 2, `tennis-europe/oop-live` 17; blocchi storici ITF certificati almeno 17.

- 1 settembre 2026 — Chiarito il conteggio D1 e lo stato del backup locale. La lista live contiene 21 nomi soltanto perché include la tabella interna Cloudflare `_cf_KV`; le tabelle applicative remote sono 20 e coincidono numericamente con le 20 tabelle applicative del SQLite locale. Sul locale `PRAGMA integrity_check=ok`. Quindi il confronto 20 contro 21 non indica un backup obsoleto. L'identità esatta del contenuto dello snapshot locale rispetto al D1 live corrente non è certificata, e il live può legittimamente essere avanzato dopo lo snapshot del 31 agosto.

- 1 settembre 2026 — Registrati gli impatti effettivi delle modifiche di audit. I file interessati sono `.github/workflows/courtwatch-cloudflare-access-audit.yml`, `ops/cloudflare-access-audit-request.json`, `ops/cloudflare-access-audit-result.json`, `tools/backup/backup-courtwatch.sh` e `tools/backup/fingerprint-sqlite.py`. Non sono stati modificati codice applicativo, runtime Worker, migrazioni, motori FITP, ITF o Tennis Europe. I path filter del workflow di deploy dell'app non includono questi file di audit/backup, quindi tali modifiche non attivano il deploy app API/D1. L'estensione dello script di backup e il nuovo fingerprint tool erano fuori dallo scope originario di sola verifica: incidono soltanto sui futuri backup locali e sulle verifiche read-only, non sul funzionamento live dell'app.

- 1 settembre 2026 — Regola operativa consolidata: una richiesta di verifica/controllo è read-only salvo autorizzazione esplicita a modificare. Per workflow GitHub lunghi si avvia una sola volta il run, si fornisce il link e non si esegue polling ripetuto; l'utente controlla l'esito e comunica verde/errore. Il report di progetto viene aggiornato autonomamente dopo modifiche, certificazioni o cambi di stato significativi. Questa regola serve a ridurre consumo di assistente/tool e non riduce frequenza, prestazioni o cadenza dei motori Court Watch.

- 1 settembre 2026 — La precedente segnalazione Vercel `Deployment rate limited — retry in 24 hours` è separata dall'audit Cloudflare D1/R2 e non costituisce un fallimento dell'audit. Nessuna modifica al comportamento Vercel viene eseguita senza autorizzazione esplicita.
