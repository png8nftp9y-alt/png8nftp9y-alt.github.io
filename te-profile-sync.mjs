import{chromium}from'playwright';import fs from'node:fs/promises';const cfg=JSON.parse(await fs.readFile('players.json','utf8')),pool=JSON.parse(await fs.readFile('te-profile-pool.json','utf8')).profiles,players=cfg.players||[],norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^A-Za-z ]/g,' ').replace(/\s+/g,' ').trim().toUpperCase(),now=new Date().toISOString(),report=[];const browser=await chromium.launch({headless:true}),ctx=await browser.newContext({locale:'en-GB',timezoneId:'Europe/Rome',userAgent:'Mozilla/5.0 (compatible; CourtWatchProfileSync/3.0)'});for(const item of pool){let match=item.knownPlayerId?players.find(p=>p.id===item.knownPlayerId):null,title='',lines=[],error=null;if(!match){const page=await ctx.newPage();try{await page.goto(item.url,{waitUntil:'domcontentloaded',timeout:60000});await page.waitForTimeout(1200);const accept=page.getByText('ACCEPT',{exact:true});if(await accept.count())await accept.first().click({timeout:5000}).catch(()=>{});await page.waitForTimeout(2500);if(/cookiewall/i.test(page.url()))await page.goto(item.url,{waitUntil:'networkidle',timeout:60000}).catch(()=>{});await page.waitForTimeout(2500);title=await page.title();const body=await page.locator('body').innerText().catch(()=>''),hay=norm(title+' '+body);lines=body.split('\n').map(x=>x.trim()).filter(Boolean).slice(0,80);const candidates=players.filter(p=>[p.name,...(p.aliases||[])].filter(x=>String(x).trim().split(/\s+/).length>1).map(norm).some(a=>a.length>5&&hay.includes(a)));if(candidates.length===1)match=candidates[0];}catch(e){error=String(e.message||e).slice(0,300)}finally{await page.close()}}if(match){match.officialUrls??={fitp:[],tennisEurope:[],itf:[]};match.officialUrls.tennisEurope=[item.url];match.profileSync??={};match.profileSync.tennisEurope={status:'synced',profileId:item.id,url:item.url,lastChecked:now};report.push({id:item.id,url:item.url,status:'synced',playerId:match.id,playerName:match.name,title});}else report.push({id:item.id,url:item.url,status:'unmatched',title,bodyLines:lines,error});}await browser.close();cfg.profileSyncUpdatedAt=now;await fs.writeFile('players.json',JSON.stringify(cfg,null,2)+'\n');await fs.writeFile('te-profile-sync.json',JSON.stringify({generatedAt:now,profiles:report},null,2)+'\n');console.log(JSON.stringify(report,null,2));
