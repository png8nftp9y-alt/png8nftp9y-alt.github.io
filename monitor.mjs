import{chromium}from'playwright';import fs from'node:fs/promises';import{URL}from'node:url';const config=JSON.parse(await fs.readFile(new URL('./players.json',import.meta.url),'utf8'));let old={};try{old=JSON.parse(await fs.readFile(new URL('./data.json',import.meta.url),'utf8'))}catch{}const SOURCES=[{id:'fitp',name:'FITP PUC',seeds:['https://www.fitp.it/Tornei/Ricerca-tornei'],max:12},{id:'tennis-europe',name:'Tennis Europe',seeds:['https://www.tenniseurope.org/tournaments','https://te.tournamentsoftware.com/find'],max:18},{id:'itf',name:'ITF pubblico',seeds:['https://www.itftennis.com/en/tournament-calendar/world-tennis-tour-juniors-calendar/'],max:18},{id:'tennistalker',name:'TennisTalker pubblico',seeds:config.players.map(p=>p.tennisTalkerUrl).filter(Boolean),max:4}];const nav=/tournament|torneo|tabellon|draw|entry|acceptance|order.of.play|ordine.di.gioco|schedule|programma|calendar|calendario|player|giocatore/i;const signals={order_of_play:/order of play|ordine di gioco|programma incontri|schedule/i,registration:/iscritt[oaie]|entry list|acceptance list|accepted|main draw|qualifying|alternate/i,draw:/tabellone|draw|round of|quarti|semifinale|finale/i,match:/campo|court|vs\.?|contro|ore\s+\d{1,2}[:.]\d{2}/i};const norm=v=>(v||'').replace(/\s+/g,' ').trim(),abs=(h,b)=>{try{return new URL(h,b).href.split('#')[0]}catch{return null}},allowed=(u,s)=>{try{const h=new URL(u).hostname.replace(/^www\./,'');return s.id==='fitp'?h.endsWith('fitp.it'):s.id==='tennis-europe'?h.endsWith('tenniseurope.org')||h.endsWith('tournamentsoftware.com'):s.id==='itf'?h.endsWith('itftennis.com'):h.endsWith('tennistalker.it')}catch{return false}},kind=t=>Object.entries(signals).find(([,r])=>r.test(t))?.[0]||'mention',clip=(t,a)=>{const up=t.toUpperCase();let i=-1;for(const x of a){i=up.indexOf(x.toUpperCase());if(i>=0)break}return i<0?'':norm(t.slice(Math.max(0,i-220),i+420))},score=l=>{const v=`${l.url} ${l.text}`;return(nav.test(v)?5:0)+(/2026|2027/.test(v)?2:0)+(/ital|ita|milano|roma|lecco|lombard/i.test(v)?3:0)-(/login|privacy|cookie|news|facebook|instagram|youtube/i.test(v)?8:0)};const browser=await chromium.launch({headless:true}),ctx=await browser.newContext({locale:'it-IT',timezoneId:'Europe/Rome',userAgent:'CourtWatchPublicMonitor/1.0'}),findings=[],tracked=new Set(old.trackedUrls||[]),sources=[];for(const source of SOURCES){const q=[...new Set([...source.seeds,...(old.trackedUrls||[]).filter(u=>allowed(u,source))])].map(url=>({url,d:0})),seen=new Set(),errors=[];while(q.length&&seen.size<source.max){const x=q.shift();if(!x?.url||seen.has(x.url)||!allowed(x.url,source))continue;seen.add(x.url);const p=await ctx.newPage();try{await p.goto(x.url,{waitUntil:'domcontentloaded',timeout:30000});await p.waitForTimeout(2000);for(const label of['Accetta','Accetta tutto','Accept','Accept all']){const b=p.getByRole('button',{name:label,exact:false}).first();if(await b.count()){await b.click({timeout:700}).catch(()=>{});break}}await p.waitForTimeout(700);const title=norm(await p.title()),text=await p.locator('body').innerText({timeout:8000}).catch(()=>''),url=p.url();for(const player of config.players){const snippet=clip(text,player.aliases||[player.name]);if(!snippet)continue;const type=kind(snippet),key=`${player.id}|${url}|${type}`;if(!findings.some(f=>f.key===key)){findings.push({key,playerId:player.id,playerName:player.name,sourceId:source.id,sourceName:source.name,type,title:title||url,url,snippet,observedAt:new Date().toISOString()});tracked.add(url)}}if(x.d<1){const links=await p.locator('a[href]').evaluateAll(ns=>ns.map(a=>({href:a.getAttribute('href'),text:(a.textContent||'').trim()})));const c=links.map(l=>({url:abs(l.href,url),text:norm(l.text)})).filter(l=>l.url&&allowed(l.url,source)&&score(l)>0).sort((a,b)=>score(b)-score(a));for(const l of c.slice(0,source.max))if(!seen.has(l.url))q.push({url:l.url,d:x.d+1})}}catch(e){errors.push({url:x.url,error:String(e.message||e).slice(0,180)})}finally{await p.close()}}sources.push({id:source.id,name:source.name,status:seen.size?(errors.length<seen.size?'ok':'partial'):'error',pagesChecked:seen.size,errorCount:errors.length,diagnostics:errors.slice(0,5)})}await browser.close();const players=config.players.map(p=>({id:p.id,name:p.name,club:p.club||null,findingCount:findings.filter(f=>f.playerId===p.id).length}));await fs.writeFile(new URL('./data.json',import.meta.url),JSON.stringify({generatedAt:new Date().toISOString(),mode:'public-web',players,findings,trackedUrls:[...tracked].slice(0,200),sources},null,2)+'\n');console.log(`CourtWatch: ${findings.length} riscontri pubblici.`);
