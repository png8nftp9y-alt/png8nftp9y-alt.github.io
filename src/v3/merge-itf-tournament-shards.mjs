import fs from 'node:fs/promises';import {NOW,FROM,REGIONS,readJson,writeJson} from './itf-common.mjs';
const dir='dist/v3/shards/itf',expected=[...Object.keys(REGIONS),'other'];let files=[];try{files=(await fs.readdir(dir)).filter(f=>/^tournaments-.+\.json$/.test(f))}catch{}
const byId=new Map(),shards=[],errors=[];let until='';for(const f of files){const d=await readJson(`${dir}/${f}`,{});until=until||d.coverageUntil||'';shards.push({shard:d.shard,status:d.status,tournamentsFound:(d.tournaments||[]).length,errors:(d.errors||[]).length});if(!String(d.status).includes('complete'))errors.push({type:'incomplete_shard',shard:d.shard,status:d.status});for(const t of d.tournaments||[]){const old=byId.get(t.competitionId)||{};byId.set(t.competitionId,{...old,...t,firstSeenAt:old.firstSeenAt||d.generatedAt,lastSeenAt:d.generatedAt})}}
for(const s of expected)if(!shards.some(x=>x.shard===s))errors.push({type:'missing_shard',shard:s});const tournaments=[...byId.values()].filter(t=>t.endDate>=FROM).sort((a,b)=>a.startDate.localeCompare(b.startDate)||a.competitionId.localeCompare(b.competitionId));if(tournaments.length<100)errors.push({type:'too_few_tournaments',count:tournaments.length,minimum:100});
const previous=await readJson('dist/v3/source_itf_tournaments.json',{tournaments:[]}),previousIds=new Set((previous.tournaments||[]).filter(t=>t.endDate>=FROM).map(t=>t.competitionId)),currentIds=new Set(tournaments.map(t=>t.competitionId)),retained=[...previousIds].filter(x=>currentIds.has(x)).length,retention=previousIds.size?retained/previousIds.size:1;if(previousIds.size>=100&&retention<.9)errors.push({type:'continuity_regression',previous:previousIds.size,retained,retention});
const out={version:1,generatedAt:NOW,status:errors.length?'itf_global_tournament_map_blocked':'itf_global_tournament_map_complete',coverageFrom:FROM,coverageUntil:until,shards,tournamentsFound:tournaments.length,continuity:{previous:previousIds.size,retained,retention},tournaments,errors};await writeJson('dist/v3/source_itf_tournaments.json',out);await writeJson('dist/v3/source_itf_tournaments_audit.json',{...out,tournaments:tournaments.slice(0,300)});console.log(JSON.stringify({...out,tournaments:undefined},null,2));if(errors.length)process.exit(2);
