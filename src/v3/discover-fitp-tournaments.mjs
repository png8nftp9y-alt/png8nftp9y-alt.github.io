import fs from 'node:fs/promises';
const NOW=new Date().toISOString();
const FROM='2025-12-18';
const BASE='https://dp-myfit-test-function-v2.azurewebsites.net';
const TENNIS_DISCIPLINE='4332';
const SOURCES=[{cod:'1',name:'TORNEI FITP'},{cod:'3',name:'CAMPIONATI A SQUADRE'}];
async function writeJson(p,v){await fs.mkdir(p.split('/').slice(0,-1).join('/'),{recursive:true});await fs.writeFile(p,JSON.stringify(v,null,2)+'\n')}
async function post(path,body){const r=await fetch(BASE+path,{method:'POST',headers:{'content-type':'application/json','user-agent':'Mozilla/5.0 CourtWatch-v3-tournament-discovery/1.0','origin':'https://www.fitp.it','referer':'https://www.fitp.it/Tornei/Ricerca-tornei'},body:JSON.stringify(body)});const t=await r.text();if(!r.ok)throw Error(r.status+' '+t.slice(0,160));return t?JSON.parse(t):null}
function it(d){return `${String(d.getUTCDate()).padStart(2,'0')}/${String(d.getUTCMonth()+1).padStart(2,'0')}/${d.getUTCFullYear()}`}
function iso(v){const s=String(v||'');let m=s.match(/^(20\d{2})-(\d{2})-(\d{2})/);if(m)return m[0];m=s.match(/(\d{1,2})\D(\d{1,2})\D(20\d{2})/);return m?`${m[3]}-${m[2].padStart(2,'0')}-${m[1].padStart(2,'0')}`:''}
function norm(v){return String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase()}
function wrongCircuit(name){const n=norm(name);if(/CAMPIONATI ITALIANI|QUALIFICAZIONE AI CAMPIONATI|CAMPIONATI REGIONALI/.test(n))return false;return /^TE\s|\bTENNIS EUROPE\b|\bTEJT\b|\bITF\b|CORETENNIS|\b(PESCARA|CREMA|SASSUOLO|AGNO|KOPER|CORREGGIO)\b/.test(n)}
const start0=new Date(FROM+'T00:00:00Z'), limit=new Date(Date.now()+420*864e5);
const specs=[];for(let s=new Date(start0);s<=limit;s=new Date(s.getTime()+14*864e5)){const e=new Date(Math.min(limit.getTime(),s.getTime()+13*864e5));for(const source of SOURCES)specs.push({start:s,end:e,source})}
function payload(s,skip){return{guid:'',profilazione:'',freetext:'',id_regione:'',id_provincia:'',id_stato:'',id_disciplina:TENNIS_DISCIPLINE,sesso:'',data_inizio:it(s.start),data_fine:it(s.end),tipo_competizione:'',categoria_eta:'',id_classifica:'',classifica:'',massimale_montepremi:'',id_area_regionale:'',ambito:'',cod_fonte:s.source.cod,id_fonte:s.source.name,rowstoskip:skip,fetchrows:100,sortcolumn:'',sortorder:''}}
const rows=[],queries=[],errors=[];let si=0;
async function worker(){while(si<specs.length){const s=specs[si++];let skip=0,total=Infinity;while(skip<total&&skip<5000){try{const r=await post('/api/v3/tornei/puc/list',payload(s,skip));const got=r?.competizioni||[];total=Number(r?.record||got.length||0);const official=got.filter(x=>x.guid&&x.guid!=='11111111-1111-1111-1111-111111111111'&&String(x.id_disciplina||'')===TENNIS_DISCIPLINE&&['1','3'].includes(String(x.cod_fonte||''))&&!wrongCircuit(x.nome_torneo||''));rows.push(...official);queries.push({source:s.source.cod,start:it(s.start),end:it(s.end),skip,rows:got.length,official:official.length,total});if(got.length<100)break;skip+=100}catch(e){errors.push({source:s.source.cod,start:it(s.start),error:e.message});break}}}}
await Promise.all(Array.from({length:10},worker));
const tournaments=[...new Map(rows.map(r=>[String(r.guid).toUpperCase(),r])).values()].map(r=>({circuit:'fitp',competitionId:r.guid,tournamentName:r.nome_torneo||'',location:[r.citta,r.sigla_provincia||r.provincia].filter(Boolean).join(' '),startDate:iso(r.data_inizio),endDate:iso(r.data_fine),disciplineId:String(r.id_disciplina||''),sourceCode:String(r.cod_fonte||''),sourceName:r.id_fonte||'',categoryAge:r.cat_eta||'',categoryClass:r.cat_class||'',tournamentType:r.tipo_torneo||'',club:r.tennisclub||'',onlineRegistration:r.iscrizione_online,authorized:r.torneo_autorizzato,sourceUrl:'https://www.fitp.it/Tornei/Dettaglio-Competizione?competitionId='+encodeURIComponent(r.guid),discoveryMethod:'structured_puc_tournament_list_only',dependsOnDraw:false,dependsOnResults:false,lastSeen:NOW})).filter(t=>!t.endDate||t.endDate>=FROM);
const out={version:'cw-v3-agenda-first',generatedAt:NOW,status:'fitp_tournament_discovery_complete_list_only_no_draws',source:'FITP P.U.C. tournament search only: id_disciplina=4332 tennis; cod_fonte=1 Tornei FITP or 3 Campionati a squadre; no keyword; no draws; no order of play; no results',coverageFrom:FROM,queries:queries.length,tournamentsFound:tournaments.length,bySource:tournaments.reduce((a,t)=>{a[t.sourceName||t.sourceCode]=(a[t.sourceName||t.sourceCode]||0)+1;return a},{}),tournaments,errors};
await writeJson('dist/v3/source_fitp_tournaments.json',out);
console.log(JSON.stringify({...out,tournaments:undefined},null,2));
