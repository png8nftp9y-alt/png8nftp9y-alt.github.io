import fs from 'node:fs/promises';
const NOW=new Date().toISOString();
const BASE='https://dp-myfit-test-function-v2.azurewebsites.net';
const TERMS=['IMPERIA','MILANO 3','MILANO3','MILANO TRE','TENNIS CLUB MILANO 3','TC MILANO 3','SPORTING MILANO 3','SPORTING MI3','BASIGLIO','TENNIS CLUB IMPERIA','TC IMPERIA','KINDER','KINDER JOY','KINDER JOY OF MOVING'];
const RAW_TERMS=['IMPERIA KINDER','KINDER IMPERIA','TENNIS TROPHY IMPERIA','TENNIS CLUB IMPERIA KINDER','MILANO 3 KINDER','KINDER MILANO 3','MILANO3 KINDER','BASIGLIO KINDER','SPORTING MILANO 3 KINDER','SPORTING MI3 KINDER','TENNIS TROPHY MILANO 3','TENNIS TROPHY BASIGLIO',...TERMS];
const CARDS={virginia_rossoni:'6701107987',alessio_nava:'9076909095'};
const norm=v=>String(v||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase();
const card=v=>String(v||'').replace(/\D+/g,'');
async function readJson(p,f){try{return JSON.parse(await fs.readFile(p,'utf8'))}catch{return f}}
async function writeJson(p,v){await fs.mkdir(p.split('/').slice(0,-1).join('/'),{recursive:true});await fs.writeFile(p,JSON.stringify(v,null,2)+'\n')}
async function post(path,body){const r=await fetch(BASE+path,{method:'POST',headers:{'content-type':'application/json','user-agent':'Mozilla/5.0 CourtWatch-v3-fitp-missing-diagnostic/1.1-raw-search','origin':'https://www.fitp.it','referer':'https://www.fitp.it/Tornei/Ricerca-tornei'},body:JSON.stringify(body)});const t=await r.text();if(!r.ok)throw Error(r.status+' '+t.slice(0,180));return t?JSON.parse(t):null}
function collectParticipants(node,into=[]){if(!node||typeof node!=='object')return into;if(Array.isArray(node)){for(const x of node)collectParticipants(x,into);return into}const name=[node.Name,node.FirstName,node.Nome].filter(Boolean).join(' '),surname=[node.Surname,node.LastName,node.Cognome].filter(Boolean).join(' ');const full1=`${name} ${surname}`.trim(),full2=`${surname} ${name}`.trim();const membershipCard=card(node.MembershipCard||node.Tessera||node.CardNumber||node.NumeroTessera||node.FitpCardNumber);if(full1||full2||membershipCard)into.push({full1,full2,membershipCard,ranking:node.Ranking||node.Classifica||'',subscriptionDate:node.SubscriptionDate||''});for(const [k,v] of Object.entries(node)){if(/result|score|winner|loser|match/i.test(k))continue;collectParticipants(v,into)}return into}
function basePayload(){return{guid:'',profilazione:'',freetext:'',id_regione:'',id_provincia:'',id_stato:'',id_disciplina:'4332',sesso:'',data_inizio:'18/12/2025',data_fine:'10/08/2028',tipo_competizione:'',categoria_eta:'',id_classifica:'',classifica:'',massimale_montepremi:'',id_area_regionale:'',ambito:'',cod_fonte:'1',id_fonte:'TORNEI FITP',rowstoskip:0,fetchrows:100,sortcolumn:'',sortorder:''}}
const rawSearch=[];
for(const term of RAW_TERMS){try{const p=basePayload();p.freetext=term;const j=await post('/api/v3/tornei/puc/list',p);const rows=(j?.competizioni||[]).map(r=>({competitionId:String(r.guid||'').toUpperCase(),tournamentName:r.nome_torneo||'',location:[r.citta,r.sigla_provincia||r.provincia].filter(Boolean).join(' '),club:r.tennisclub||'',disciplineId:String(r.id_disciplina||''),sourceCode:String(r.cod_fonte||''),sourceName:r.id_fonte||'',startDate:r.data_inizio||'',endDate:r.data_fine||''}));rawSearch.push({term,total:j?.record||rows.length,rows:rows.slice(0,120)});}catch(e){rawSearch.push({term,error:e.message})}}
const map=await readJson('dist/v3/source_fitp_tournaments.json',{tournaments:[]});
const mapIds=new Set((map.tournaments||[]).map(t=>String(t.competitionId||'').toUpperCase()));
const candidates=(map.tournaments||[]).filter(t=>{const s=norm([t.tournamentName,t.location,t.club,t.sourceName].join(' '));return TERMS.some(term=>s.includes(norm(term)))});
for(const rs of rawSearch){for(const r of (rs.rows||[])){if(r.competitionId&&!mapIds.has(r.competitionId)){candidates.push({...r,coverageModes:['raw_not_in_map_'+rs.term],sourceUrl:'https://www.fitp.it/Tornei/Dettaglio-Competizione?competitionId='+encodeURIComponent(r.competitionId)})}}}
const uniqCandidates=[];const seen=new Set();for(const c of candidates){const id=String(c.competitionId||'').toUpperCase();if(!id||seen.has(id))continue;seen.add(id);uniqCandidates.push(c)}
const inspected=[];
for(const t of uniqCandidates){const rec={competitionId:t.competitionId,tournamentName:t.tournamentName,location:t.location,club:t.club,startDate:t.startDate,endDate:t.endDate,coverageModes:t.coverageModes||[],inMap:mapIds.has(String(t.competitionId||'').toUpperCase()),matchedTerms:TERMS.filter(term=>norm([t.tournamentName,t.location,t.club].join(' ')).includes(norm(term)))};try{const d=await post('/api/v3/puc/competizione/dettaglio',{competitionUid:t.competitionId});const participants=[];for(const draw of (d?.Tournaments||[])){for(const p of collectParticipants(draw.Participants||[]))participants.push({...p,draw:draw.TournamentDescription||draw.Description||''})}rec.draws=(d?.Tournaments||[]).map(x=>x.TournamentDescription||x.Description||'');rec.participantsCount=participants.length;rec.cardHits=Object.fromEntries(Object.entries(CARDS).map(([name,c])=>[name,participants.filter(p=>p.membershipCard===c).map(p=>({draw:p.draw,full1:p.full1,full2:p.full2,ranking:p.ranking,subscriptionDate:p.subscriptionDate}))]));rec.nearNameHits=participants.filter(p=>/ROSSONI|NAVA/i.test(`${p.full1} ${p.full2}`)).slice(0,30);}catch(e){rec.error=e.message}inspected.push(rec)}
const out={generatedAt:NOW,mapGeneratedAt:map.generatedAt,tournamentsFound:map.tournamentsFound,mapCandidates:candidates.length,inspectedCount:inspected.length,terms:TERMS,rawTerms:RAW_TERMS,cards:CARDS,rawSearch,inspected};
await writeJson('dist/v3/fitp_missing_diagnostic.json',out);
console.log(JSON.stringify({...out,rawSearch:rawSearch.map(x=>({term:x.term,total:x.total,error:x.error,rows:(x.rows||[]).length,notInMap:(x.rows||[]).filter(r=>!mapIds.has(r.competitionId)).length})),inspected:out.inspected.filter(x=>!x.inMap||x.cardHits?.virginia_rossoni?.length||x.cardHits?.alessio_nava?.length||/IMPERIA|MILANO 3|MI3|BASIGLIO/i.test(`${x.tournamentName} ${x.location} ${x.club}`)).map(x=>({competitionId:x.competitionId,tournamentName:x.tournamentName,location:x.location,club:x.club,inMap:x.inMap,matchedTerms:x.matchedTerms,participantsCount:x.participantsCount,cardHits:x.cardHits,nearNameHits:x.nearNameHits,error:x.error}))},null,2));
