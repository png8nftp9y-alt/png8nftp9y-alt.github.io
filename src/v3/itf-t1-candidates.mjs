export function persistedTournament(previous={}){const combo=(previous.eventInventory||[]).map(x=>x?.combo).find(x=>x?.sourceUrl)||{};const sourceUrl=previous.sourceUrl||previous.drawsResultsUrl||combo.sourceUrl||'';return{competitionId:previous.competitionId,tournamentName:previous.tournamentName||'',startDate:previous.startDate||'',endDate:previous.endDate||'',sourceUrl,drawsResultsUrl:previous.drawsResultsUrl||sourceUrl,persistedPending:true}}
export function t1Candidates(mapTournaments=[],stateTournaments={},today,tomorrow,requested=[]){const byId=new Map(mapTournaments.map(t=>[t.competitionId,t]));for(const previous of Object.values(stateTournaments||{}))if(previous?.decision==='pending'&&previous.competitionId&&!byId.has(previous.competitionId)){const fallback=persistedTournament(previous);if(fallback.sourceUrl)byId.set(fallback.competitionId,fallback)}const wanted=new Set(requested.map(x=>String(x).toUpperCase()));return[...byId.values()].filter(t=>{const previous=stateTournaments?.[t.competitionId];return previous?.decision==='pending'||(!previous&&t.startDate<=tomorrow&&t.endDate>=today)}).filter(t=>!wanted.size||wanted.has(String(t.competitionId).toUpperCase()))}
