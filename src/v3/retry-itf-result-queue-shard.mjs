import fs from 'node:fs/promises';
import {gzipSync,gunzipSync} from 'node:zlib';
import {NOW,TODAY,hash,readJson,tournamentEvents,drawsheet,playerFromApi} from './itf-common.mjs';

const INDEX=Number(process.env.ITF_RETRY_SHARD||0),TOTAL=Number(process.env.ITF_RETRY_TOTAL||8),TARGET_ID=String(process.env.ITF_RETRY_COMPETITION_ID||'').trim(),dir='dist/v3/shards/itf';
const map=await readJson('dist/v3/source_itf_tournaments.json',{tournaments:[]}),byId=new Map((map.tournaments||[]).map(t=>[t.competitionId,t]));
const originalByCompetition=new Map(),matches=[],players=new Map(),retryQueue=[],resolvedKeys=[],sections=[];
const eventKey=c=>[c.playerTypeCode,c.matchTypeCode,c.eventClassificationCode,c.drawsheetStructureCode].join('-');
const retryKey=x=>x.event?`${x.competitionId}|draw|${x.event}`:`${x.competitionId}|events`;
function score(team){return(team?.scores||[]).filter(Boolean).map(s=>s.scoreDisplay??s.score??s.games??'').filter(x=>x!=='').join(' ')}
function parse(json,t,c){const out=[];for(const group of[...(json.koGroups||[]),...(json.rrGroups||[])])for(const round of group.rounds||group.matchesByRound||[])for(const m of round.matches||[]){const teams=m.teams||[];if(!teams.length)continue;const parsed=teams.map(team=>({players:(team.players||[]).map(playerFromApi),score:score(team),isWinner:Boolean(team.isWinner),entryStatus:team.entryStatus||''}));for(const team of parsed)for(const p of team.players)if(p.name)players.set(p.id||p.name,p);out.push({competitionId:t.competitionId,tournamentName:t.tournamentName,startDate:t.startDate,endDate:t.endDate,eventId:json.eventId||'',event:eventKey(c),playerTypeCode:c.playerTypeCode,matchTypeCode:c.matchTypeCode,eventClassificationCode:c.eventClassificationCode,drawsheetStructureCode:c.drawsheetStructureCode,round:String(round.roundDesc||round.roundName||''),roundNumber:round.roundNumber??null,matchId:String(m.matchId||''),playStatus:m.playStatus||'',resultStatus:m.resultStatus||'',teams:parsed,winnerTeam:parsed.findIndex(x=>x.isWinner),sourceUrl:t.drawsResultsUrl||t.sourceUrl,observedAt:NOW})}return out}

for(const file of await fs.readdir(dir))if(/^results-\d+\.json\.gz$/.test(file)){try{const d=JSON.parse(gunzipSync(await fs.readFile(`${dir}/${file}`)));for(const item of d.retryQueue||[]){if(!item?.competitionId||(TARGET_ID?item.competitionId!==TARGET_ID:hash(item.competitionId)%TOTAL!==INDEX))continue;const list=originalByCompetition.get(item.competitionId)||[];if(!list.some(x=>retryKey(x)===retryKey(item)))list.push(item);originalByCompetition.set(item.competitionId,list)}}catch{}}
for(const [id,original] of originalByCompetition){
 const t=byId.get(id);if(!t){for(const item of original)retryQueue.push({...item,error:'missing_tournament_metadata'});continue}
 let combos;try{combos=await tournamentEvents(t);resolvedKeys.push(`${id}|events`)}catch(error){for(const item of original)retryQueue.push({...item,error:error.message});continue}
 const wanted=new Set(original.filter(x=>x.event).map(x=>x.event)),inventory=original.some(x=>!x.event),selected=inventory?combos:combos.filter(c=>wanted.has(eventKey(c))),selectedKeys=new Set(selected.map(eventKey));
 for(const event of wanted)if(!selectedKeys.has(event))retryQueue.push({competitionId:id,event,type:'draw_retryable',error:'event_not_returned_by_GetEventFilters'});
 for(const c of selected){const event=eventKey(c),key=`${id}|draw|${event}`;try{const parsed=parse(await drawsheet(c),t,c),playerNames=new Set(parsed.flatMap(m=>m.teams||[]).flatMap(team=>team.players||[]).map(p=>String(p.name||'').trim()).filter(Boolean)),concluded=Boolean(t.endDate&&t.endDate<TODAY);matches.push(...parsed);if(playerNames.size){resolvedKeys.push(key);sections.push({competitionId:id,event,status:'populated',playerNames:playerNames.size,matches:parsed.length})}else if(concluded){retryQueue.push({competitionId:id,event,type:'concluded_empty_draw_anomaly',error:'concluded_draw_has_no_player_names'});sections.push({competitionId:id,event,status:'published_empty_anomaly',playerNames:0,matches:parsed.length})}else{resolvedKeys.push(key);sections.push({competitionId:id,event,status:'published_empty_pending',playerNames:0,matches:parsed.length})}}catch(error){retryQueue.push({competitionId:id,event,type:'draw_retryable',error:error.message});sections.push({competitionId:id,event,status:'missing_or_unreadable',playerNames:0,matches:0,error:error.message})}}
}
const unique=[...new Map(matches.map(m=>[m.matchId||[m.competitionId,m.event,m.roundNumber,m.round].join('|'),m])).values()],unresolved=[...new Map(retryQueue.map(x=>[retryKey(x),x])).values()];
const output=TARGET_ID?`tournament-${TARGET_ID.replace(/[^A-Za-z0-9._-]+/g,'_')}.json.gz`:`retry-${INDEX}.json.gz`;
await fs.writeFile(`${dir}/${output}`,gzipSync(JSON.stringify({version:2,generatedAt:NOW,shard:TARGET_ID||INDEX,competitionId:TARGET_ID||null,targetCount:originalByCompetition.size,originalRetryCount:[...originalByCompetition.values()].flat().length,resolvedKeys:[...new Set(resolvedKeys)],sections,players:[...players.values()],matches:unique,retryQueue:unresolved})));
console.log(JSON.stringify({shard:INDEX,tournaments:originalByCompetition.size,originalRetries:[...originalByCompetition.values()].flat().length,resolved:[...new Set(resolvedKeys)].length,populated:sections.filter(x=>x.status==='populated').length,publishedEmptyPending:sections.filter(x=>x.status==='published_empty_pending').length,concludedEmptyAnomalies:sections.filter(x=>x.status==='published_empty_anomaly').length,missing:sections.filter(x=>x.status==='missing_or_unreadable').length,matches:unique.length,remaining:unresolved.length},null,2));
