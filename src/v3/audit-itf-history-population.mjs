import fs from 'node:fs/promises';
import path from 'node:path';
import {gunzipSync} from 'node:zlib';

const root=process.env.ITF_HISTORY_AUDIT_ROOT||'dist/v3/shards/itf/draw-tasks',expected=Number(process.env.ITF_EXPECTED_TASKS||4291),files=[];
async function walk(dir){let items=[];try{items=await fs.readdir(dir,{withFileTypes:true})}catch{return}for(const item of items){const full=path.join(dir,item.name);if(item.isDirectory())await walk(full);else if(item.name.endsWith('.json.gz'))files.push(full)}}
await walk(root);
const byTask=new Map(),unreadable=[];
for(const file of files)try{const doc=JSON.parse(gunzipSync(await fs.readFile(file)));if(doc?.taskId)byTask.set(doc.taskId,doc);else unreadable.push({file,error:'taskId_missing'})}catch(error){unreadable.push({file,error:error.message})}
const docs=[...byTask.values()],retry=docs.filter(doc=>doc.status!=='complete'),complete=docs.filter(doc=>doc.status==='complete'),emptyPlayers=complete.filter(doc=>!(doc.players||[]).length),emptyPlayersAndMatches=emptyPlayers.filter(doc=>!(doc.matches||[]).length),playersWithoutMatches=complete.filter(doc=>(doc.players||[]).length&&!(doc.matches||[]).length),missing=Math.max(0,expected-byTask.size);
const family=event=>String(event||'').split('-').slice(0,3).join('-'),familyDocs=new Map();
for(const doc of complete){const key=doc.competitionId+'|'+family(doc.event),list=familyDocs.get(key)||[];list.push(doc);familyDocs.set(key,list)}
const embeddedPlayers=doc=>(doc.matches||[]).flatMap(match=>match.teams||[]).flatMap(team=>team.players||[]),item=doc=>({taskId:doc.taskId,competitionId:doc.competitionId,event:doc.event,players:(doc.players||[]).length,matches:(doc.matches||[]).length,embeddedMatchPlayers:embeddedPlayers(doc).length,embeddedNamedPlayers:embeddedPlayers(doc).filter(player=>String(player?.name||'').trim()).length,status:doc.status,error:doc.error||''});
const emptyDetails=emptyPlayers.map(doc=>{const siblings=(familyDocs.get(doc.competitionId+'|'+family(doc.event))||[]).filter(other=>other.taskId!==doc.taskId),populatedSibling=siblings.find(other=>(other.players||[]).length>0);return{...item(doc),family:family(doc.event),alternativeStructure:Boolean(populatedSibling),populatedSibling:populatedSibling?{taskId:populatedSibling.taskId,event:populatedSibling.event,players:(populatedSibling.players||[]).length,matches:(populatedSibling.matches||[]).length}:null}}),coveredAlternatives=emptyDetails.filter(item=>item.alternativeStructure),unresolvedEmpty=emptyDetails.filter(item=>!item.alternativeStructure);
const report={version:2,generatedAt:new Date().toISOString(),criterion:'populated_requires_at_least_one_player_or_populated_sibling_structure',status:missing||retry.length||unreadable.length?'history_population_audit_incomplete_inputs':unresolvedEmpty.length?'history_contains_unresolved_empty_draws':'history_all_declared_families_populated',expected,files:files.length,uniqueTasks:byTask.size,complete:complete.length,retry:retry.length,missing,unreadable:unreadable.length,populated:complete.length-emptyPlayers.length,emptyPlayers:emptyPlayers.length,emptyPlayersAndMatches:emptyPlayersAndMatches.length,playersWithoutMatches:playersWithoutMatches.length,coveredAlternativeStructures:coveredAlternatives.length,unresolvedEmptyDraws:unresolvedEmpty.length,emptyDraws:emptyDetails,unresolved:unresolvedEmpty,retryTasks:retry.map(item),unreadableFiles:unreadable};
await fs.mkdir('dist/v3/audits',{recursive:true});await fs.writeFile('dist/v3/audits/itf-history-population-audit.json',JSON.stringify(report,null,2)+'\n');
console.log(JSON.stringify({...report,emptyDraws:undefined,retryTasks:undefined,unreadableFiles:undefined},null,2));
if(missing||retry.length||unreadable.length)process.exitCode=2;
