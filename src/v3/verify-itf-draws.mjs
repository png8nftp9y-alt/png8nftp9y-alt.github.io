import {NOW,TODAY,readJson,writeJson,tournamentEvents,drawsheet,playerFromApi,norm,aliases} from './itf-common.mjs';
const file='dist/v3/source_itf_entries.json',doc=await readJson(file,{entries:[]}),players=(await readJson('players.json',{players:[]})).players||[],map=await readJson('dist/v3/source_itf_tournaments.json',{tournaments:[]}),byId=new Map((map.tournaments||[]).map(t=>[t.competitionId,t])),entries=[],audit=[];let confirmed=0,removed=0,inconclusive=0;
function shiftedStart(start){const time=Date.parse(start||'');return Number.isFinite(time)?new Date(time-2*864e5).toISOString().slice(0,10):''}
function days(start){return start?Math.floor((Date.parse(TODAY)-Date.parse(start))/864e5):-999}
function names(json){const out=[];for(const group of[...(json.koGroups||[]),...(json.rrGroups||[])])for(const round of group.rounds||group.matchesByRound||[])for(const m of round.matches||[])for(const team of m.teams||[])for(const p of team.players||[]){const x=playerFromApi(p);if(x.name)out.push(x.name)}return out}
for(const e of doc.entries||[]){const controlStartDate=shiftedStart(e.startDate),d=days(controlStartDate);if(d<-1){entries.push(e);audit.push({playerId:e.playerId,competitionId:e.competitionId,controlStartDate,daysFromControlStart:d,decision:'kept_pre_tournament_acceptance'});continue}const t=byId.get(e.competitionId)||e;let allNames=[],finalNames=[],failures=[],phases=0;try{const combos=(await tournamentEvents(t)).filter(c=>c.matchTypeCode==='S');for(const c of combos)try{const ns=names(await drawsheet(c));allNames.push(...ns);phases++;if(c.eventClassificationCode==='M')finalNames.push(...ns)}catch(err){failures.push({phase:[c.playerTypeCode,c.eventClassificationCode,c.drawsheetStructureCode].join('-'),error:err.message})}}catch(err){failures.push({error:err.message})}const p=players.find(p=>p.id===e.playerId),hit=p&&allNames.some(n=>aliases(p).some(a=>norm(n)===a)),reliable=failures.length===0&&finalNames.length>=4;if(hit){entries.push({...e,acceptanceCode:'',acceptancePosition:null,calendarListLabel:'',entryStatus:'draw_confirmed',calendarState:'draw_confirmed'});confirmed++;audit.push({playerId:e.playerId,competitionId:e.competitionId,controlStartDate,daysFromControlStart:d,decision:'confirmed_in_official_singles_draw',players:allNames.length,phases})}else if(reliable){removed++;audit.push({playerId:e.playerId,competitionId:e.competitionId,controlStartDate,daysFromControlStart:d,decision:'removed_reliable_complete_main_singles_draw_absence',players:allNames.length,finalPlayers:finalNames.length,phases})}else{entries.push(e);inconclusive++;audit.push({playerId:e.playerId,competitionId:e.competitionId,controlStartDate,daysFromControlStart:d,decision:'kept_inconclusive_draw',players:allNames.length,finalPlayers:finalNames.length,phases,failures})}}
await writeJson(file,{...doc,generatedAt:NOW,entriesFound:entries.length,entries});await writeJson('dist/v3/source_itf_draw_audit.json',{generatedAt:NOW,today:TODAY,summary:{mode:'live_t_minus_1',originalEntries:(doc.entries||[]).length,entriesFound:entries.length,confirmedInDraw:confirmed,removedByReliableDrawAbsence:removed,inconclusiveKept:inconclusive},audit});console.log(JSON.stringify({confirmed,removed,inconclusive,entries:entries.length},null,2));
