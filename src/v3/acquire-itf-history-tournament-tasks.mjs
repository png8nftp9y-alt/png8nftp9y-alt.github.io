import fs from 'node:fs/promises';import {gzipSync} from 'node:zlib';import {NOW,bootstrapTournamentSession,drawsheet,playerFromApi} from './itf-common.mjs';
const raw=process.env.ITF_TOURNAMENT_TASKS_JSON;if(!raw)throw new Error('ITF_TOURNAMENT_TASKS_JSON_required');const tasks=JSON.parse(raw);if(!Array.isArray(tasks)||!tasks.length)throw new Error('ITF_tournament_tasks_required');if(new Set(tasks.map(x=>x.competitionId)).size!==1)throw new Error('ITF_tasks_must_share_competitionId');await fs.mkdir('dist/v3/shards/itf/draw-tasks',{recursive:true});await bootstrapTournamentSession({sourceUrl:tasks[0].sourceUrl});let unresolved=0;function score(team){return(team?.scores||[]).filter(Boolean).map(s=>s.scoreDisplay??s.score??s.games??'').filter(x=>x!=='').join(' ')}
for(const task of tasks){const players=new Map(),matches=[];let json=null,error='';try{json=await drawsheet({...task,sessionReady:true})}catch(e){error=e.message}if(json)for(const group of[...(json.koGroups||[]),...(json.rrGroups||[])])for(const round of group.rounds||group.matchesByRound||[])for(const m of round.matches||[]){const teams=(m.teams||[]).map(team=>({players:(team.players||[]).map(playerFromApi),score:score(team),isWinner:Boolean(team.isWinner),entryStatus:team.entryStatus||''}));for(const team of teams)for(const p of team.players)if(p.name)players.set(p.id||p.name,p);matches.push({competitionId:task.competitionId,event:task.event,round:String(round.roundDesc||round.roundName||''),roundNumber:round.roundNumber??null,matchId:String(m.matchId||''),playStatus:m.playStatus||'',resultStatus:m.resultStatus||'',teams,winnerTeam:teams.findIndex(x=>x.isWinner),sourceUrl:task.sourceUrl,observedAt:NOW})}const requirePopulated=String(process.env.ITF_REQUIRE_POPULATED_DRAW||'')==='1',empty=requirePopulated&&!error&&players.size===0,finalError=error||(empty?'draw_not_published_or_incomplete':'');const out={version:2,generatedAt:NOW,status:finalError?'retry':'complete',failureType:error?'technical_error':empty?'not_published_or_incomplete':null,taskId:task.taskId,competitionId:task.competitionId,event:task.event,players:[...players.values()],matches,error:finalError};await fs.writeFile(`dist/v3/shards/itf/draw-tasks/${task.taskId}.json.gz`,gzipSync(JSON.stringify(out)));console.log(JSON.stringify({taskId:task.taskId,players:players.size,matches:matches.length,status:out.status,failureType:out.failureType,error:finalError}));if(out.status!=='complete')unresolved++}if(unresolved)process.exitCode=2;
