import fs from'node:fs/promises';const v=Date.now();let h=await fs.readFile('index.html','utf8');h=h.replace(/<script src="app\.js(?:\?v=[^"]*)?"><\/script>/,`<script src="app.js?v=${v}"></script>`).replace(/<link rel="stylesheet" href="styles\.css(?:\?v=[^"]*)?">/,`<link rel="stylesheet" href="styles.css?v=${v}">`);await fs.writeFile('index.html',h);let a=await fs.readFile('app.js','utf8');a=a.replace(/load\(\);setInterval\(load,12e4\);?$/,"load();setInterval(load,30000);addEventListener('focus',load);addEventListener('visibilitychange',()=>{if(!document.hidden)load()});");await fs.writeFile('app.js',a);const vercel={version:2,cleanUrls:true,trailingSlash:false,headers:[{source:'/(.*)',headers:[{key:'Cache-Control',value:'no-store, max-age=0, must-revalidate'}]},{source:'/data.json',headers:[{key:'Cache-Control',value:'no-store, max-age=0, must-revalidate'},{key:'Content-Type',value:'application/json; charset=utf-8'}]},{source:'/players.json',headers:[{key:'Cache-Control',value:'no-store, max-age=0, must-revalidate'},{key:'Content-Type',value:'application/json; charset=utf-8'}]}],rewrites:[{source:'/',destination:'/index.html'}]};await fs.writeFile('vercel.json',JSON.stringify(vercel,null,2)+'\n');await fs.writeFile('deploy-version.json',JSON.stringify({version:v,at:new Date().toISOString()},null,2)+'\n');
